mutation getUser($citizenId: String!) {
  user(citizenId: $citizenId) {
    user {
      id
      citizenId
      lastName
      firstName
      phone
      email
      birthDate
      reservations {
        id
        qrText
        meetingId
      }
    }
    errors {
      message
    }
  }
}
