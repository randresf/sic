mutation getUser($citizenId: String!) {
  user(document: $citizenId) {
    user {
      id
      document
      lastName
      firstName
      phone
      email
      birthDate
      reservations {
        id
        qrText
        meetingId
      }
    }
    errors {
      message
    }
  }
}
