mutation addReservation($input: ReservationType!) {
  addReservation(data: $input) {
    reservation {
      id
      qrText
      meeting {
        id
        title
        meetingDate
    }
    errors {
      message
    }
  }
}
