export type FormikType = {
  field: any,
  form: {
    errors: any,
    touched: any
  }
}

export type CheckboxPropsType = {
  text: string,
  color: string
}

export type meetingType = {
  cedula: string,
  nombre: string,
  apellido: string,
  telefono: number | string
  correo: string,
  date: string
}